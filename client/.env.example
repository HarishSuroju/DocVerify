# Frontend API base URL
# For local dev with same-origin proxy leave empty and use /api fallback.
# For deployed frontend, set full backend URL like: https://your-backend.onrender.com/api
VITE_API_BASE_URL=
