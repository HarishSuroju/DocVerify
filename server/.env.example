# Server
PORT=
NODE_ENV=

# Database
MONGO_URI=

# JWT
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=
ACCESS_TOKEN_EXPIRY=
REFRESH_TOKEN_EXPIRY=
JWT_ISSUER=
JWT_AUDIENCE=

# Application-layer encryption (32-byte base64 or 64-char hex key)
DATA_ENCRYPTION_KEY=
DATA_ENCRYPTION_KEY_ID=
# Optional keyring JSON for rotation, e.g. {"2026-q1":"<base64-key>","2026-q2":"<base64-key>"}
DATA_ENCRYPTION_KEYS=

# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=

# Email (Nodemailer) — P1
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Frontend URL (CORS)
CLIENT_URL=
